(function (factory) {
    if (typeof module === 'object' && typeof module.exports === 'object') {
        var v = factory(require, exports); if (v !== undefined) module.exports = v;
    }
    else if (typeof define === 'function' && define.amd) {
        define(["require", "exports", 'dojo-compose/compose', './createDestroyable'], factory);
    }
})(function (require, exports) {
    "use strict";
    var compose_1 = require('dojo-compose/compose');
    var createDestroyable_1 = require('./createDestroyable');
    function isRenderable(value) {
        return value && typeof value.render === 'function';
    }
    exports.isRenderable = isRenderable;
    var createRenderable = compose_1.default({
        render: null,
        tagName: 'div'
    }, function (instance, options) {
        if (options && options.tagName) {
            instance.tagName = options.tagName;
        }
    })
        .mixin({
        mixin: createDestroyable_1.default,
        initialize: function (instance, options) {
            if (options) {
                if (options.render) {
                    instance.render = options.render;
                }
                if (options.tagName) {
                    instance.tagName = options.tagName;
                }
                if (options.parent) {
                    options.parent.append(instance);
                }
            }
        }
    });
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.default = createRenderable;
});
//# sourceMappingURL=../_debug/mixins/createRenderable.js.map